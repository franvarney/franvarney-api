const {expect} = require('code') // eslint-disable-line
const Lab = require('lab')

const lab = exports.lab = Lab.script()
const {describe, it} = lab // eslint-disable-line

const GitHub = require('../../server/handlers/github') // eslint-disable-line
